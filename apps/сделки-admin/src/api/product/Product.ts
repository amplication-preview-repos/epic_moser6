import { Delivery } from "../delivery/Delivery";

export type Product = {
  id: string;
  createdAt: Date;
  updatedAt: Date;
  name: string | null;
  description: string | null;
  price: number | null;
  deliveries?: Array<Delivery>;
};
